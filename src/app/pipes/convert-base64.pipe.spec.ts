import { ConvertBase64Pipe } from './convert-base64.pipe';

describe('ConvertBase64Pipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertBase64Pipe();
    expect(pipe).toBeTruthy();
  });
});
